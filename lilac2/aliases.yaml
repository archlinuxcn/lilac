python:
  source: alpm
  alpm: python
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
python2:
  source: alpm
  alpm: python2
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
ruby:
  source: alpm
  alpm: ruby
  from_pattern: ^(\d+\.\d+)\..*
  to_pattern: \1
perl:
  source: alpm
  alpm: perl
  from_pattern: ^(\d+\.\d+)\..*
  to_pattern: \1
r:
  source: alpm
  alpm: r
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
lua:
  source: alpm
  alpm: lua
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
boost:
  source: alpm
  alpm: boost-libs
  from_pattern: ^(\d+\.\d+)\..*
  to_pattern: \1
icu:
  source: alpm
  alpm: icu
  from_pattern: ^(\d+)\..*
  to_pattern: \1
readline:
  source: alpm
  alpm: readline
  from_pattern: ^(\d+)\..*
  to_pattern: \1
clang:
  source: alpm
  alpm: clang
  from_pattern: ^(\d+)\..*
  to_pattern: \1
protobuf:
  source: alpm
  alpm: protobuf
  provided: libprotobuf.so
  strip_release: true
jsoncpp:
  source: alpm
  alpm: jsoncpp
  provided: libjsoncpp.so
  strip_release: true
mediawiki:
  source: alpm
  alpm: mediawiki
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
grpc:
  source: alpm
  alpm: grpc
  provided: libgrpc++.so
  strip_release: true
