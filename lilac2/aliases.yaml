python:
  archpkg: python
  from_pattern: '^(\d+\.\d+)\..*'
  to_pattern: '\1'
ruby:
  archpkg: ruby
  from_pattern: ^(\d+\.\d+)\..*
  to_pattern: \1
boost:
  archpkg: boost-libs
  from_pattern: ^(\d+\.\d+)\..*
  to_pattern: \1
icu:
  archpkg: icu
  from_pattern: ^(\d+)\..*
  to_pattern: \1
readline:
  archpkg: readline
  from_pattern: ^(\d+)\..*
  to_pattern: \1
clang:
  archpkg: clang
  from_pattern: ^(\d+)\..*
  to_pattern: \1
protobuf:
  # soname changes with patch versions too; see https://www.archlinux.org/todo/protobuf-361-rebuild/
  archpkg: protobuf
  strip-release: true
